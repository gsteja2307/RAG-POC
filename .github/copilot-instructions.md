# Workspace Instructions

- Do not read, search, open, summarize, quote, or inspect `.env` files or other environment-variable files unless the user explicitly asks for that.
- Treat environment files as secret-bearing inputs and avoid using tools that reveal their contents by default.
- If a task requires environment configuration, explain the expected variable names without reading existing env file contents.